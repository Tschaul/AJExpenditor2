import * as React from "react";

export let ChartView = class ChartView extends React.Component {
    render() {
        return React.createElement(
            "div",
            null,
            "Hello from ChartView"
        );
    }
};