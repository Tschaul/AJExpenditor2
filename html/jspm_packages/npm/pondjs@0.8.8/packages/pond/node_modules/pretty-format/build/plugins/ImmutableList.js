/* */ 
'use strict';
const printImmutable = require('./lib/printImmutable');
const IS_LIST = '@@__IMMUTABLE_LIST__@@';
const test = (maybeList) => !!(maybeList && maybeList[IS_LIST]);
const print = (val, print, indent, opts, colors) => printImmutable(val, print, indent, opts, colors, 'List', false);
module.exports = {
  print,
  test
};
