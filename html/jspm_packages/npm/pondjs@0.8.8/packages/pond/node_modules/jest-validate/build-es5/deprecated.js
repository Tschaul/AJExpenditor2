/* */ 
'use strict';
var _require = require('./utils'),
    logValidationWarning = _require.logValidationWarning,
    DEPRECATION = _require.DEPRECATION;
var deprecationMessage = function deprecationMessage(message, options) {
  var comment = options.comment;
  var name = options.title && options.title.deprecation || DEPRECATION;
  logValidationWarning(name, message, comment);
};
var deprecationWarning = function deprecationWarning(config, option, deprecatedOptions, options) {
  if (option in deprecatedOptions) {
    deprecationMessage(deprecatedOptions[option](config), options);
    return true;
  }
  return false;
};
module.exports = {deprecationWarning: deprecationWarning};
