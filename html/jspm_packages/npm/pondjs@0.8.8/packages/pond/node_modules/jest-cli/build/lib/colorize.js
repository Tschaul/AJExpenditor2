/* */ 
'use strict';
const chalk = require('../../../chalk/index');
module.exports = (str, start, end) => chalk.dim(str.slice(0, start)) + chalk.reset(str.slice(start, end)) + chalk.dim(str.slice(end));
