/* */ 
'use strict';
const chalk = require('../../chalk/index');
const format = (value) => require('../../pretty-format/build/index')(value, {min: true});
const deprecatedOptions = {
  preprocessorIgnorePatterns: (options) => `  Option ${chalk.bold('"preprocessorIgnorePatterns"')} was replaced by ${chalk.bold('"transformIgnorePatterns"')}, which support multiple preprocessors.

  Jest now treats your current configuration as:
  {
    ${chalk.bold('"transformIgnorePatterns"')}: ${chalk.bold(`${format(options.preprocessorIgnorePatterns)}`)}
  }

  Please update your configuration.`,
  scriptPreprocessor: (options) => `  Option ${chalk.bold('"scriptPreprocessor"')} was replaced by ${chalk.bold('"transform"')}, which support multiple preprocessors.

  Jest now treats your current configuration as:
  {
    ${chalk.bold('"transform"')}: ${chalk.bold(`{".*": ${format(options.scriptPreprocessor)}}`)}
  }

  Please update your configuration.`,
  testPathDirs: (options) => `  Option ${chalk.bold('"testPathDirs"')} was replaced by ${chalk.bold('"roots"')}.

  Jest now treats your current configuration as:
  {
    ${chalk.bold('"roots"')}: ${chalk.bold(format(options.testPathDirs))}
  }

  Please update your configuration.
  `
};
module.exports = deprecatedOptions;
