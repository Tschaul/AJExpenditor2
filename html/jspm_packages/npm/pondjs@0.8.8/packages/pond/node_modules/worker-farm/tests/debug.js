/* */ 
(function(process) {
  'use strict';
  const workerFarm = require('../lib/index'),
      workers = workerFarm(require.resolve('./child'), ['args']);
  workers.args(function() {
    workerFarm.end(workers);
    console.log('FINISHED');
    process.exit(0);
  });
})(require('process'));
